import React from 'react';
import { getVideo } from '../api/fetch';

const Video = ({ video }) => {
    return (
        <div className='video'>
            
        </div>
    );
}

export default Video;
