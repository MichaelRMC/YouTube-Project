import React from 'react';
import { getVideo } from '../api/fetch';
import Navbar from './Navbar';

const Video = ({ video }) => {
    return (
        <>
       
        <div className='video'>
            
        </div>
        </>
    );
}

export default Video;
